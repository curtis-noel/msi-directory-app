<%= form_for(@notice) do |f| %>
<% if @notice.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@notice.errors.count, "error") %> prohibited this notice from being saved:</h2>
  <ul>
    <% @notice.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<!--Button Group-->
<%= render partial: 'partials/admin_btn_group', locals: {:back_url => 'notices', :new_url => 'notices', :new => false} %>
<br/>

<div class="panel panel-default">
  <div class="panel-body">
    <div class="rows">
      <div class="row">
        <div class="form-group col-xs-12 col-sm-6 col-md-4 ttip">
          <%= f.label :content %><br/>
          <%= f.text_area :content %>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="btn-group">
  <%= f.submit :class => "btn" %>
</div>
<% end %>

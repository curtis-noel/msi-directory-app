<%= form_for(@grouping) do |f| %>
<% if @grouping.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@grouping.errors.count, "error") %> prohibited this card from being saved:</h2>
  <ul>
    <% @grouping.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<!--Button Group-->
<%= render partial: 'partials/admin_btn_group', locals: {:back_url => 'groupings', :new_url => 'groupings', :new => false} %>
<br/>

<div class="panel panel-default">
  <div class="panel-body">
    <div class="rows">
      <div class="row">
        <div class="form-group col-xs-12 col-sm-6 col-md-4 ttip">
          <%= f.label :name %><br/>
          <%= f.text_field :name %>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="btn-group">
  <%= f.submit :class => "btn" %>
</div>
<% end %>
